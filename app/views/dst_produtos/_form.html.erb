<%= form_for(dst_produto) do |f| %>
  <% if dst_produto.errors.any? %>
    <div id="error_explanation" class="card-panel red white-text">
      <h5>Resolva os problemas a seguir antes de Salvar:</h5>

      <ul>
      <% dst_produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="card-panel">
  
  <div class="input-field">

    <%= f.label :nome %>
    <%= f.text_field :nome %>
    
  </div>

  <div class="input-field">

    <%= f.label :descricao %>
    <%= f.text_area :descricao %>
    
  </div>

  <div class="input-field">

    <%= f.label :unidade_medida, "Unidade de Medida de Saída" %>
    <%= f.text_field :unidade_medida %>
    
  </div>

</div>
  <div class="actions">
    <%= f.submit "Salvar", class:"btn" %>
  </div>
<% end %>
